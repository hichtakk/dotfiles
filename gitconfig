[user]
useConfigOnly = true

[color]
ui = auto

[color "status"]
added = blue
changed = yellow
untracked = red

[core]
editor = vim

[push]
default = simple

[filter "lfs"]
clean = git-lfs clean %f
smudge = git-lfs smudge %f
required = true

[ghq]
root = ~/src
root = ~/.golang.d
